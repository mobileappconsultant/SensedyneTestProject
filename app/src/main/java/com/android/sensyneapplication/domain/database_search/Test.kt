fun main() {
    var bla = "NQM01"
    var bla2 = "NLL185"
/*    var isValid =
        bla2.filter { Character.isDigit(it) }.count() in 2..3
                && bla2.filter { Character.isDigit(it).not() }
            .count() in 2..3

    println(isValid)

    fun checkBla(string: String) {
        var acceptedCharacterLength = 3
        var currentDigitCount = string.filter { Character.isDigit(it) }.count()
        if (currentDigitCount > 2) {
            acceptedCharacterLength = 2
        }

    }*/
}
